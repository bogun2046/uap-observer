# WP8 R2 冻结设计基线

- 日期：2026-08-21（Asia/Shanghai）
- 仓库：`bogun2046/uap-observer`
- 父基线：G7-R2 已验收提交 `a34acd3282001421de1376e6e62ca3d7cf0f4233`
- 前置 CI：<https://github.com/bogun2046/uap-observer/actions/runs/32388610012>
- 冻结标准：`G8-FROZEN-20260821-02`
- 实施编号：`WP8-IMPL-20260821-02`
- 验收编号：`WP8-ACCEPT-20260821-02`
- 文档状态：**Frozen by Codex / 可用于实施授权**
- 编码门禁：**仍关闭，直至项目负责人发出 WP8.1 启动口令**

## 冻结提交与起点

本目录所在的 docs-only Git 提交是 WP8 的设计根，记为 `G8_R2_DESIGN_SHA`。该 SHA 只能在提交形成后解析，因此不回写到本文件，避免 Git 自引用。

WP8.1 的唯一合法起点是：

```text
项目负责人启动口令中明确列出的 G8_R2_DESIGN_SHA
```

它必须满足：

1. 提交的第一父提交为 `a34acd3282001421de1376e6e62ca3d7cf0f4233`；
2. 相对父提交只新增或修改 `docs/wp8/**`；
3. `SHA256SUMS` 对除自身外的冻结文档逐项通过；
4. 项目负责人将完整 SHA、阶段编号、任务书和验收用例一并发送给实施者。

禁止 WP8.1 再从 G7 SHA 直接分叉。WP8.2–WP8.6 只能从上一阶段的已验收 SHA 继续。

## 本提交范围

仅包含架构与验收文档：

- 没有业务代码；
- 没有 Alembic 迁移；
- 没有 CI、Makefile 或覆盖率配置变更；
- 没有生产数据或对象存储变更。

## 角色与放行

- Codex 负责架构冻结与独立技术审核。
- Grok 负责按冻结设计实施当前获准阶段，不得改 ADR 或验收口径。
- 项目负责人拥有唯一阶段放行权。
- Codex 的“技术上具备进入下一阶段条件”不会自动授权 Grok 继续；必须由项目负责人给出明确启动口令。

在启动口令前，Grok 只能保持停止状态。每个 WP8.x 交付固定 SHA 后必须停止，等待项目负责人转交 Codex 审核。

## 权威顺序

发生冲突时按以下顺序解释：

1. 本冻结标准下的 ADR；
2. `implementation-ticket.md`；
3. `acceptance-ticket.md` 与 `acceptance-cases.md`；
4. G7 固定 SHA 中的已验收约束；
5. 实施者说明、自测报告或对话推断。

任何无法按该顺序消解的冲突均关闭编码门禁，由 Codex 出具设计勘误或新冻结编号。
