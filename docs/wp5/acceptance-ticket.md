# WP5 独立验收单

- 验收编号：`WP5-ACCEPT-20260816-01`
- 冻结标准：`G5-FROZEN-20260816-01`
- 状态：G5 通过
- 验收责任：数据负责人 + QA
- 前置门禁：G4 已通过
- 门禁：WP6 开启

## 独立性要求

- 使用全新测试数据库和对象存储，不复用开发者本地采集结果。
- 使用提交中的固定 RSS 快照和独立运行夹具；真实网络来源仅作为补充演练。
- 至少连续执行同一 RSS 任务两次，并独立核对 source run、artifact、document 和 job 结果。
- 使用真实 `uap_worker` 连接验证采集任务；不得使用超级用户替代业务角色。
- 独立保存测试日志、SQL 结果、固定快照哈希、源码提交和证据清单哈希。

## 验收范围

- G5-01 Collector 接口边界与版本化输入输出。
- G5-02 固定 RSS 快照确定性解析。
- G5-03 URL、来源条目 ID 和 artifact 幂等去重。
- G5-04 source run 记录和任务状态一致性。
- G5-05 304、空结果、403、429、超时和 5xx 分类。
- G5-06 限速、冷却和来源健康检查。
- G5-07 单一来源失败不影响其他来源。
- G5-08 来源和采集批次可追溯。

## 通过规则

- G5-01 至 G5-08 全部通过，且静态检查、全量测试、迁移检查和证据清单完整。
- 任一用例失败，登记为“独立验收不通过，待整改后复验”，WP6 门禁保持关闭。

## 最终记录字段

## 独立复验与归档记录

- 复验轮次：`G5-R4`，结论为技术复验通过，未发现新的代码或运行态阻断项。
- 验收环境：全新 PostgreSQL、对象存储和业务角色；另按 required 顺序执行 `WP3 → WP4 → WP5`。
- 验收基线：归档提交 `7c3e2816bfaf8c21c5c91baf889c63f5746a4572`；对应 required CI run `32086401130` 已完成且全绿。
- PR：[#28](https://github.com/bogun2046/uap-observer/pull/28)，保持 Draft 不影响冻结标准验收。
- 迁移版本：`0001 → 0007_source_run_lease_guard`；重复升级、降级回升和失败收口均通过。
- 固定快照：`platform/tests/fixtures/rss/g5-fixed-feed.xml`，SHA-256 为 `b3a998a48fecd9c18bfb75d294a60465aad12a55490b1c72e6629ebcf9dd73c8`。
- 逐项结果：G5-01 至 G5-08 全部通过；59/59 测试通过，覆盖率 83.98%；Ruff、mypy、迁移、权限及 WP2/WP3/WP4/WP5 检查均通过。
- 关键运行态结果：source run、artifact、document、job 追溯链成立；成功、空结果、304、403、429、超时和 5xx 分类及任务状态闭环成立；迟到 Worker checkpoint 返回 SQLSTATE `40001` 且不改变已完成 source run；对象登记锁和一致性扫描竞态验证通过。
- 证据清单：42/42 项校验通过；归档提交后的清单 SHA-256 为 `ffb8fbbd2bb2eddf1615c4d51ede34ec92db6ecb37f7d6056864ba687650f9c4`。
- 验收责任：数据负责人 + QA；验收时间：2026-08-18；缺陷：G5-R1 至 G5-R3 已整改并关闭。
- 阶段结论：`G5 通过，WP6 开启`。
