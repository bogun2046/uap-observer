# G1 第二轮独立复验不通过记录

- 验收编号：`G1-REJECT-R2-20260812-01`
- 前序整改：`G1-REMEDIATION-20260812-01`
- 结论：第二轮复验不通过，待补充整改
- 门禁：WP2 继续关闭
- 复验日期：2026-08-12（Asia/Shanghai）

## 用例结论

| 用例 | 第二轮复验结论 |
|---|---|
| G1-01 数据分层 | 不通过 |
| G1-02 任务状态解耦 | 通过 |
| G1-03 主键、唯一约束与所有者 | 不通过 |
| G1-04 AI 多版本追加 | 通过 |
| G1-05 公开角色最小权限 | 不通过 |
| G1-06 PostgreSQL 唯一权威 | 通过 |
| G1-07 单一迁移源 | 通过 |
| G1-08 交付物一致性 | 不通过 |

## 新增阻断缺陷

- `G1-R2-D01`：C4 容器图、时序图、部署说明和代码布局仍由普通 Worker 执行 public 投影，与 `uap_worker` 无 public 权限、独立 `uap_publisher` 写入的权限模型冲突；时序图还把 `model_run` 错写到 core。
- `G1-R2-D02`：`entity_aliases.source_document_version_id` 和三张标签关联表的 `origin_analysis_result_id` 缺少明确外键声明。
- `G1-R2-D03`：`public.relations.relation_grant_id` 未声明唯一；授权撤回决定未以 `(withdrawn_by_decision_id, review_case_id)` 复合外键约束到同一审核案件。

## 补充整改要求

修复上述三项，增加针对 Publisher 边界、遗漏外键、关系授权唯一性及撤回案件一致性的语义验证，生成独立第二轮整改证据和 SHA-256 清单后再申请复验。本记录永久保留“不通过”结论。

## 后续状态

2026-08-12 已完成补充整改并提交第三次独立复验。本记录继续永久保留第二轮“不通过”结论，不因整改自检通过而改写历史验收结果；第三次复验通过前 WP2 继续关闭。
