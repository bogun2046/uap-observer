# G1 独立验收不通过记录

- 验收编号：`G1-REJECT-20260811-01`
- 冻结标准：`G1-FROZEN-20260811-01`
- 验收结论：不通过，待整改复验
- 门禁：暂不得进入 WP2
- 验收日期：2026-08-11（Asia/Shanghai）
- 整改状态：2026-08-12 已完成整改并提交复验；本记录仍保留原“不通过”结论。

## 用例结论

| 用例 | 独立验收结论 |
|---|---|
| G1-01 数据分层 | 不通过 |
| G1-02 任务状态解耦 | 通过 |
| G1-03 主键、唯一约束与所有者 | 不通过 |
| G1-04 AI 多版本追加 | 部分满足，不通过 |
| G1-05 公开角色最小权限 | 不通过 |
| G1-06 PostgreSQL 唯一权威 | 通过 |
| G1-07 单一迁移源 | 通过 |
| G1-08 交付物一致性 | 不通过 |

## 阻断缺陷

- `G1-D01`：`DocumentDetail`、`EntityDetail`、`ReviewCaseDetail` 的 `allOf` 与分支级 `additionalProperties: false` 冲突，正常对象无法通过 JSON Schema 校验。
- `G1-D02`：对象存储缺少统一物理对象登记，artifact version 不能共享同域同哈希对象；派生正文错误引用 ingest artifact version。
- `G1-D03`：发布授权只覆盖文档，不能证明公开实体和关系经过审核；缺少公开文档—实体关联表。
- `G1-D04`：数据库权限矩阵同时授予普通 Worker 公开写权限并声称仅 Publisher 可写，存在自相矛盾。
- `G1-D05`：analysis selection 缺少同文档、同 result type 的数据库复合约束；实体候选缺少来源 analysis result 的关联模型。
- `G1-D06`：原 18 项自动检查偏重结构计数和关键词，没有实例级 OpenAPI Schema、跨文档约束和权限一致性检查。

## 复验要求

修复全部阻断缺陷，更新设计自检记录，重新生成独立的整改证据与 SHA-256 清单，并提交 G1 复验；原自动自检通过记录不得作为 G1 通过结论。
