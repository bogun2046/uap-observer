# G2 独立验收不通过记录

- 验收编号：`G2-REJECT-20260812-01`
- 对应实现：`WP2-IMPL-20260812-01`
- 结论：不通过；存在 1 项安全阻断缺陷，必要运行态验收未完成
- 门禁：WP3 继续关闭
- 验收日期：2026-08-12（Asia/Shanghai）

## 用例结论

| 用例 | 结论 |
|---|---|
| G2-01 一个命令启动开发环境 | 阻塞：未实际启动 Docker 和空 volume |
| G2-02 空环境自动创建数据库和对象桶 | 阻塞：未实际执行空 volume 初始化 |
| G2-03 CI 失败关闭 | 不通过：缺少镜像漏洞门禁；required check 和失败注入未实测 |
| G2-04 密钥边界 | 部分通过：静态措施成立；镜像历史、运行日志和容器 Gitleaks 未实测 |
| G2-05 版本一致性与安全补丁 | 不通过：主版本一致，但 Python、PostgreSQL 和 MinIO 安全补丁基线不合格 |
| G2-06 staging 可重复部署 | 阻塞：未连续执行两次真实部署 |

## 阻断缺陷

- `G2-D01`：版本源固定的 Python 3.12.11、PostgreSQL 16.10 和 MinIO `RELEASE.2025-09-07` 未达到当前安全补丁基线；CI 只扫描 Python 包、源码和密钥，没有扫描最终应用及全部服务镜像，High/Critical 漏洞可以错误放行。

## 已确认成立

- 原 35/35 文件清单及分离式锚点通过，清单 SHA-256 为 `eac9cfb908d151cd23b4bf6bbaefff74394bf67209a1482e03e4f76bb91185d3`。
- 18/18 策略检查、9/9 平台测试、95.19% 覆盖率和 81/81 旧系统测试通过。
- Ruff、mypy、Bandit、Shell 和原 65 包联网 pip-audit 通过。
- 随机 env、0600 权限与重复执行在临时环境成立。

## 整改要求

升级 Python 与 PostgreSQL；将受影响 MinIO 替换为受支持且修复的构建或维护中的 S3 兼容方案；为最终应用和全部固定服务镜像增加 High/Critical 失败关闭扫描。整改后提交 Git 固定版本，并完成 Docker 空环境、required gate/失败注入及 staging 双次部署实测。本记录永久保留“不通过”结论。
